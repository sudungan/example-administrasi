const { defineComponent, ref, watch, onMounted } = Vue

export default defineComponent({
    name: "cardTotalJp",
    props: { },
    emits: [''],
    setup(props) {

    },
    template: `
    <p> 10 </p>
    `
});
