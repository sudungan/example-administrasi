const { defineComponent, ref, watch, computed } = Vue

export default defineComponent({
    name: 'dataTableMajor',
    props: {},
    setup(props) {

        return {

        }
    },
    template: `

    `
});
